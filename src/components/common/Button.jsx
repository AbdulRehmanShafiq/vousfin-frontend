/* eslint-disable react-refresh/only-export-components */
/*
 * DEPRECATED — this light-theme duplicate now re-exports the canonical
 * ui/Button (which accepts both variant vocabularies: primary/secondary and
 * gradient/outline). Kept only so legacy imports keep working; new code should
 * import '@/components/ui/Button' directly.
 */
export { default } from '../ui/Button'
export { Button } from '../ui/Button'
